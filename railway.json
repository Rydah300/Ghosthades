{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev && mkdir -p storage && chmod 755 storage"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT -t ."
  }
}